version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: seazone
      POSTGRES_PASSWORD: seazone
      POSTGRES_DB: seazone
    ports:
      - '5432:5432'
    volumes:
      - seazone_data:/var/lib/postgresql/data

volumes:
  seazone_data:
